# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# AI Provider Configuration
# Options: 'claude-cli' | 'anthropic' | 'openai'
AI_PROVIDER=claude-cli

# Anthropic API (only needed if AI_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your_anthropic_api_key
# Anthropic Model
# Modelos disponibles:
#   - claude-opus-4-20250514      (más capaz, más lento, más caro)
#   - claude-sonnet-4-20250514    (balanceado) - default
#   - claude-haiku-4-5            (más reciente, rápido y económico)
#   - claude-3-5-haiku-20241022   (versión estable anterior)
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# OpenAI API (only needed if AI_PROVIDER=openai)
OPENAI_API_KEY=your_openai_api_key
# OpenAI Model
# Modelos disponibles:
#   - gpt-4o        (más capaz) - default
#   - gpt-4o-mini   (más rápido y económico)
#   - gpt-4-turbo   (versión anterior)
OPENAI_MODEL=gpt-4o

# Resend (for email forward feature)
# Get your API key at https://resend.com
RESEND_API_KEY=re_your_resend_api_key
# Webhook secret for verifying inbound emails
RESEND_WEBHOOK_SECRET=whsec_your_webhook_secret
# Domain for inbound emails (configure in Resend dashboard)
RESEND_INBOUND_DOMAIN=inbound.travelr.app

# Admin Configuration
# Comma-separated list of admin email addresses
# Server-side (for API routes)
ADMIN_EMAILS=admin@example.com
# Client-side (for UI - must match ADMIN_EMAILS)
NEXT_PUBLIC_ADMIN_EMAILS=admin@example.com
